import 'package:flutter/material.dart';

class BabyBumpScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Baby bump screen'),
      ),
    );
  }
}
