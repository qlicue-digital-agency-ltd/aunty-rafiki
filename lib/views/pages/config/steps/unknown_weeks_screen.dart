import 'package:flutter/material.dart';

class UnknownWeeksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('UNKOWN'),
    );
  }
}
